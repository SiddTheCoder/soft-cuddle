/**
 * agency.softmato.com — client portal.
 *
 * Placeholder. Phase 8. Every query here must be scoped to the authenticated
 * client at the data layer, never by a URL parameter (docs/RULES.md §6).
 */
export default function PortalPage() {
  return (
    <main className="mx-auto flex max-w-2xl flex-1 flex-col justify-center px-6 py-24">
      <h1 className="text-xl font-semibold">Client portal</h1>
      <p className="mt-3 text-sm text-neutral-600">Phase 8.</p>
    </main>
  );
}
