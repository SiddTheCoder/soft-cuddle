/**
 * softmato.com — home.
 *
 * Placeholder. The real public site is Phase 2, built on CMS content and the
 * design system in docs/DESIGN.md (which is still awaiting founder approval).
 */
export default function HomePage() {
  return (
    <main className="mx-auto flex max-w-2xl flex-1 flex-col justify-center px-6 py-24">
      <h1 className="text-3xl font-semibold tracking-tight">Softmato</h1>
      <p className="mt-3 text-neutral-600">
        Software company. Public site arrives in Phase 2.
      </p>
    </main>
  );
}
